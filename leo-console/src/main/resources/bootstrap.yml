spring:
  cloud:
    config:
      name: leo-console,leo-common,common-database,common-eureka,common-jetbrick,common-ufrog
      uri: ${config.uri:http://101.132.100.91:8000}
      profile: ${config.profile:default,dev}