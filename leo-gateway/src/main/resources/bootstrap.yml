spring:
  cloud:
    config:
      name: common-database,common-eureka,common-jetbrick,common-ufrog,leo-common,leo-gateway
      uri: ${config.uri:http://101.132.100.91:8000}
      profile: ${config.profile:dev}