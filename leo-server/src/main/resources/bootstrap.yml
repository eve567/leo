spring:
  cloud:
    config:
      name: common-database,common-eureka,common-ufrog,leo-common,leo-server
      uri: ${config.uri:http://101.132.100.91:8000}
      profile: ${config.profile:default,dev}