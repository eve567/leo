spring:
  cloud:
    config:
      name: leo-server,leo-common,common-database,common-eureka,common-ufrog
      uri: ${config.uri:http://101.132.100.91:8000}
      profile: ${config.profile:default,dev}